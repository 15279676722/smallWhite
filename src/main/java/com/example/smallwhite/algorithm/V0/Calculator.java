package com.example.smallwhite.algorithm.V0;import java.util.Scanner;/** * @author: yangqiang * @create: 2021-05-21 17:23 */public class Calculator {    /**     * 键盘输入流     * */    public static  Scanner s = new Scanner(System.in);    /**     * 上一次计算完的值     * */    public static int afterInt = 0;    /**     * 是否点了清除     * */    public static boolean clearFlag = true;    public static void main(String[] args) {        while(true){            //1 继续 2 退出 3 清除            int exit = calculator();            if (exit == 2 ){                return;            }        }    }    public static int calculator(){        //先把上一次计算完的值 赋值给第一个数        int numOne = afterInt;        if(clearFlag){            System.out.print("请输入整数:");            //如果点了清除的话 读取键盘录入            numOne = s.nextInt();            // 每次操作后clearFlag=false  如果点了清除操作 则置为true 后面有            clearFlag = false;        }        System.out.print("选择运算符: 1(+) 2(-) 3(*) 4(/) :");        // 1234 +-*/        int operator = s.nextInt();        System.out.print("请输入整数:");        int numTwo = s.nextInt();        int result = 0;        String operatorStr = "";        switch (operator){            case 1:                result = numOne + numTwo;                operatorStr = "+";                break;            case 2:                result = numOne - numTwo;                operatorStr = "-";                break;            case 3:                result = numOne * numTwo;                operatorStr = "*";                break;            case 4:                result = numOne / numTwo;                operatorStr = "/";                break;            default:                break;        }        afterInt = result;        System.out.println("结果:"+ result);        System.out.println("("+numOne + operatorStr + numTwo+")");        System.out.print("1(继续) 2(退出) 3(清空)");        int exit = s.nextInt();        switch (exit){            case 1: afterInt = result; break;            case 2: break;            //相当于清空操作 再输入两次值 这里做个标记            case 3: afterInt = 0; clearFlag = true; break;        }        return exit;    }}